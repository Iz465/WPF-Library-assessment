<UserControl x:Class="WPF_Library_assessment.User_Control_Stuff.bookCardUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Library_assessment.User_Control_Stuff"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400" Background="DarkGray">
    <Grid Margin="10">
        <Border BorderBrush="Black" BorderThickness="5">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="10" Color="Black" Opacity="0.5"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="200"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="70"/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding FirstName}" HorizontalAlignment="center" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="1" Text="{Binding LastName}" HorizontalAlignment="center" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="2" Text="{Binding Username}" HorizontalAlignment="Center" VerticalAlignment="Center"/>



            </Grid>
        </Border>
    </Grid>
</UserControl>
